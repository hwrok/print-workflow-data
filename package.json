{
  "name": "print-workflow-data",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "spellcheck": "cspell -c utility/cspell/cspell.config.json --no-progress **",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "lint": "pnpm format && pnpm spellcheck",
    "lint:fix": "pnpm format:fix && pnpm spellcheck",
    "test": "bats tests/*.bats",
    "pr:check": "pnpm test && pnpm format && pnpm spellcheck"
  },
  "devDependencies": {
    "bats": "^1.13.0",
    "bats-assert": "^2.2.4",
    "bats-support": "^0.3.0",
    "cspell": "^9.6.4",
    "prettier": "^3.8.1"
  }
}
